PROVISIONERS="${PROVISIONERS}
   nornet
   prepare-development-environment
   prepare-5gVINNI-sources
"
if [ "${HYPERVISOR}" == "QEMU" ] ; then
   POSTPROCESSORS="${POSTPROCESSORS}
      maas
   "
fi
